name: kindle-scanner-infra
description: GCP infra for kindle-scanner
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-typescript
